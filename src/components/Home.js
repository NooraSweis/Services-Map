import React from 'react';

const Home = () => {
    return ( <
        div className = "search" >
        <
        input type = "text"
        placeholder = "Search..." / >
        <
        /div>
    );
}

export default Home;